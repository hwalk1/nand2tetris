CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Not (in=a, out=nota);
    Not (in=b, out=notb);
    AND (a=a, b=notb, out=aAndNotb)
    AND (a=nota, b=b, out=not)
    Or (a=aAndNotb b=notaAndb, out=out);
}